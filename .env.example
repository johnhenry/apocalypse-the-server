# Anthropic API Key (required)
ANTHROPIC_API_KEY=your-api-key-here

# Server Port (optional, defaults to 3000)
PORT=3000

# Model Configuration (optional)
# Supported models for Claude Agent SDK:
# - claude-sonnet-4-5-20250929 (recommended, best tool use capabilities)
# - claude-3-5-sonnet-20241022 (good balance of speed and capability)
# - claude-3-5-haiku-20241022 (faster, may have some limitations)
#
# WARNING: claude-3-haiku-20240307 is NOT recommended with Agent SDK
# The older Haiku model may crash due to limited tool-use capabilities
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Max Turns for Agent (optional, defaults to 20)
# Reduce this for faster models or simpler requests
MAX_TURNS=20

# File Storage Configuration (optional)
# Directory where files can be read/written by the server
# Defaults to ./files directory (which is gitignored) if not specified
# Use absolute path for clarity (e.g., /Users/yourname/data)
# FILE_STORAGE_DIR=/path/to/storage
#
# SELF-MODIFICATION MODE:
# Set FILE_STORAGE_DIR to the server's own directory to enable self-modification
# This allows Claude to read and modify its own source code files
# FILE_STORAGE_DIR=.
# ⚠️  WARNING: Self-modification can break the server if Claude makes mistakes!

# Database Configuration (optional)
# Path to SQLite database file
# Defaults to ./server-data.db if not specified
# DATABASE_PATH=/path/to/database.db
